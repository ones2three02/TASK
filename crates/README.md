# Crates

Rust crates for TASK live here.

## Directories

- `task-core/` - legacy database core crate kept for compatibility with the current workspace.
- `task-web/` - legacy web backend crate kept for compatibility with the current workspace.

The workspace root is defined in the repository-level `Cargo.toml`.
