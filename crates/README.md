# Crates

Rust crates for TASK live here.

## Directories

- `dbx-core/` - legacy database core crate kept for compatibility with the current workspace.
- `dbx-web/` - legacy web backend crate kept for compatibility with the current workspace.

The workspace root is defined in the repository-level `Cargo.toml`.
